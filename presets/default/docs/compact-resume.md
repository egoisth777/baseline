Context was just compacted. Before continuing, re-state in one line what the current
task is and what is left to do, then resume. Do not assume earlier steps were
verified — if a claim is not visible in the summary above, re-check it rather than
trusting it.
